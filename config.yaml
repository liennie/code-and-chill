server:
  port: 8080
  dataDir: data
  shutdownTimeout: 30s

db:
  file: db/woc.db

puzzles:
  default: "test"
  events:
    - path: test
      config: ../woc-test/event.yaml
    - path: test2
      config: ../woc-test/event.yaml
