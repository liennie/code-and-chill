# 401: Unauthorized

{{ $return := $.Request.URL.Query.Get "return" | default $.Request.URL.Path | trimPrefix "/" -}}
You must be [logged in](/{{ $.Event.Path }}/login{{ if $return }}?return={{ $return }}{{ end }}) to perform this action.

You can:

- [Log in](/{{ $.Event.Path }}/login{{ if $return }}?return={{ $return }}{{ end }})
- Return to the [homepage](/{{ $.Event.Path }})
- Take a break and come back later :)

If you believe this is an error, please [let us know](/{{ $.Event.Path }}/contact).
